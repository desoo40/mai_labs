- Красно-черные деревья

	Бинарное дерево плохо тем, что при определенных условиях может выродиться в линейный список.
	- Сбалансированные деревья решают эту проблему:

		* RB
			1. Корень всегда черный
			2. У красного всегда черный потомок
			3. Черная высота
			4. Все листья черные
			5. Все узлы либо красные, либо черные

			//Вставка элемента в красно-черное дерево

			enum colour = {RED, BLACK};
			typedef struct TRB_tree
			{
				int value;
				TRB_tree *left;
				TRB_tree *right;
				colour
			} TRB_tree;

			void RB_tree_node_insert(RBtree *root, Node *node)
			{
				if (root == NULL) {
					root = node;
					return;
				}

				TRB_tree *y = root;
				
				while()
			}

		* AVL
		* B
		* Patric

Корман, Сэджвик.